apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.36.0 (HEAD)
  labels:
    io.kompose.service: golang-demo
  name: golang-demo
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
#      nodePort: 31080
  selector:
    io.kompose.service: golang-demo
